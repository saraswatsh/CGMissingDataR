% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner.R
\name{run_missingness_benchmark}
\alias{run_missingness_benchmark}
\title{Run missingness benchmark}
\usage{
run_missingness_benchmark(
  data_path,
  target_col = "LBORRES",
  feature_cols = c("TimeSeries", "TimeDifferenceMinutes", "USUBJID"),
  mask_rates = c(0.05, 0.1, 0.2, 0.3, 0.4),
  test_size = 0.2,
  random_state = 42,
  imputer_random_state = 42,
  rf_n_estimators = 200,
  knn_k = 5
)
}
\arguments{
\item{data_path}{Path to a CSV file.}

\item{target_col}{Name of the target column.}

\item{feature_cols}{Character vector of feature column names.}

\item{mask_rates}{Numeric vector of missingness rates (0-1).}

\item{test_size}{Validation split fraction.}

\item{random_state}{Random seed for train/val splitting and model seeding.}

\item{imputer_random_state}{Random seed for the iterative imputer.}

\item{rf_n_estimators}{Number of trees for the random forest.}

\item{knn_k}{Number of neighbors for kNN.}
}
\value{
A data.frame with columns MaskRate, Model, MAPE, R2.
}
\description{
Loads a CSV, splits train/validation, masks feature values at various rates,
imputes via an Iterative Imputer (MICE-style), trains Random Forest and kNN
regressors, and returns MAPE and R2 per model and mask rate.

This function is a thin R wrapper over the Python implementation shipped in
\code{inst/python/CGMissingData}.
}
\examples{
data("CGMExampleData")
tmp <- tempfile(fileext = ".csv")
write.csv(CGMExampleData, tmp, row.names = FALSE)
results <- run_missingness_benchmark(tmp, mask_rates = c(0.05, 0.10))
head(results)
}
\author{
Shubh Saraswat, Hasin Shahed Shad, and Xiaohua Douglas Zhang
}
